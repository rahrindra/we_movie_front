import { Box } from "@chakra-ui/react";
import { Outlet } from "react-router-dom";

function LoginPage() {
  

  return (
    <Box
      h="100%"
      bgColor="cyan"
    >
      login page
    </Box>
  );
}

export default LoginPage;
